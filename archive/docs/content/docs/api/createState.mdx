# `createState`

You can create a state by passing a [design object](/docs/api/design/) to the `createState` function.

```js
const state = createState({ ... })
```

You can use and interact with the resulting state using the [state](/docs/api/state) API.
